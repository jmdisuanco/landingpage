{
  "name": "landingpage",
  "version": "1.0.0",
  "description": "Deployed landingpage project",
  "main": "index.js",
  "scripts": {
    "build-css": "tailwindcss -i ./src/input.css -o ./styles.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "start": "npx serve ."
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "engines": {
    "node": "18.x"
  }
}